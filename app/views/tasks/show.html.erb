<h2><%= @task.title %></h2>

<p><%= @task.details %></p>
<p><%= @task.completed %></p>

<%= link_to "Back to the list", tasks_path %> <br>
<%= link_to "Edit a task", edit_task_path %> <br>
<%= link_to "Delete a task",
  task_path(@task),
  data: { turbo_method: :delete, turbo_confirm: "Are you sure?" }
%>
